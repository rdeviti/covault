#pragma once

const int didbits = 256;                        // did bitwidth
const int hashbits = 32;                        // fingerprint hash bitwidth
const size_t tilebits = 2 * 256 + 32 + 16 + 8;  // bits of a tile in kvs


